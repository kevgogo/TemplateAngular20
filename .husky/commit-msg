MSG_FILE="${1:-.git/COMMIT_EDITMSG}"

# Inserta Items auto (A/M/D/R) después de "Corregido:"
node scripts/commit-msg-augment.js "$MSG_FILE"

# Exige Descripción con texto real (Corregido es opcional)
node scripts/commit-msg-guard.js "$MSG_FILE"

# Valida convencional (types, etc.)
npx commitlint --edit "$MSG_FILE"
